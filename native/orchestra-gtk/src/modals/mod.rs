//! Form modals that are too large for the alert/confirm/prompt shapes
//! `dialogs.rs` serves, but share its presentation (`.orch-dialog` chrome,
//! `dialog-pop` entry, the `dialogs::OPEN` stack the remote-control harness
//! walks for screenshots and Escape routing).

pub mod linear;
pub mod repo_scripts;
